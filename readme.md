## Basic project setup

This project houses a basic sass based grid system and folder structure to kick start a new project.

Uses grunt for sass compiling and a livereload server

`npm install`

`grunt` for a sass watch

`grunt server` for livereload server with sass watch